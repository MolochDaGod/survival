/**
 * Survival camp package — GRUDGES / Nexus survival era ONLY.
 * Warlords must not import from this path.
 */
export {
  CampClaimSystem,
  CLAIM_FLAG_ITEM_IDS,
  CAMP_BENCH_ITEM_IDS,
  CAMP_BUILDING_ITEM_IDS,
  CAMP_RADIUS_M,
  ERA_ID,
  type CampBuffs,
  type CampClaimSnapshot,
  type CampBenchRecord,
  type CampBuildingRecord,
} from './CampClaimSystem';
